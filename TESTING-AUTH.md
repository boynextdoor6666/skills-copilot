# 🚀 Тестирование системы авторизации

## Быстрый старт

### 1. Запуск Backend

```bash
cd movie-aggregator-backend
./mvnw spring-boot:run
```

Backend запустится на **http://localhost:8080**

### 2. Запуск Frontend

```bash
cd movie-aggregator-frontend
npm run dev
```

Frontend запустится на **http://localhost:3001**

---

## 🧪 Тестирование через UI

### Регистрация нового пользователя

1. Откройте **http://localhost:3001**
2. Нажмите **"Регистрация"** в правом верхнем углу
3. Заполните форму:
   ```
   Username: testuser
   Email: test@example.com
   Password: test123
   Подтвердите пароль: test123
   ```
4. Следите за индикаторами валидации (должны стать зелёными):
   - ✓ Минимум 6 символов
   - ✓ Содержит цифру
   - ✓ Содержит букву
   - ✓ Пароли совпадают
5. Нажмите **"Зарегистрироваться"**
6. Вас автоматически перенаправит на главную страницу
7. В навигации появится ваш username и кнопка "Выход"

### Вход существующего пользователя

1. Если вы вышли, нажмите **"Вход"**
2. Введите данные:
   ```
   Username: testuser
   Password: test123
   ```
3. Нажмите **"Войти"**
4. Успешный вход → редирект на главную

### Защищённые страницы

1. **Не авторизованный пользователь:**
   - Попробуйте открыть `/profile`
   - Вас перенаправит на `/login`

2. **Авторизованный пользователь:**
   - Нажмите на свой username в навигации
   - Откроется страница профиля
   - Можете просматривать защищённый контент

### Выход

1. Нажмите **"Выход"** в навигации
2. Токен удалится из localStorage
3. Кнопки "Вход" и "Регистрация" снова появятся

---

## 🔧 Тестирование через API (Postman/cURL)

### 1. Регистрация

```bash
POST http://localhost:8080/api/auth/register
Content-Type: application/json

{
  "username": "apiuser",
  "email": "api@example.com",
  "password": "api123"
}
```

**Ожидаемый ответ (201 Created):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "userId": 1,
  "username": "apiuser",
  "email": "api@example.com",
  "role": "USER",
  "message": "Регистрация успешна"
}
```

### 2. Вход

```bash
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "username": "apiuser",
  "password": "api123"
}
```

**Ожидаемый ответ (200 OK):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "userId": 1,
  "username": "apiuser",
  "email": "api@example.com",
  "role": "USER",
  "message": "Успешный вход"
}
```

**Сохраните токен!**

### 3. Валидация токена

```bash
GET http://localhost:8080/api/auth/validate
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Ожидаемый ответ (200 OK):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "userId": 1,
  "username": "apiuser",
  "email": "api@example.com",
  "role": "USER",
  "message": "Токен валиден"
}
```

### 4. Защищённый запрос (пример)

```bash
GET http://localhost:8080/api/profile/me
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Без токена → 401 Unauthorized**
**С токеном → 200 OK**

---

## 🐛 Тестирование ошибок

### Регистрация с существующим username

```bash
POST http://localhost:8080/api/auth/register

{
  "username": "apiuser",  # Уже существует
  "email": "new@example.com",
  "password": "password123"
}
```

**Ожидается: 400 Bad Request**
```
Имя пользователя уже занято
```

### Регистрация с существующим email

```bash
POST http://localhost:8080/api/auth/register

{
  "username": "newuser",
  "email": "api@example.com",  # Уже существует
  "password": "password123"
}
```

**Ожидается: 400 Bad Request**
```
Email уже зарегистрирован
```

### Вход с неверным паролем

```bash
POST http://localhost:8080/api/auth/login

{
  "username": "apiuser",
  "password": "wrongpassword"
}
```

**Ожидается: 401 Unauthorized**
```
Неверное имя пользователя или пароль
```

### Доступ к защищённому эндпоинту без токена

```bash
GET http://localhost:8080/api/profile/me
# Без заголовка Authorization
```

**Ожидается: 401 Unauthorized**

### Доступ с недействительным токеном

```bash
GET http://localhost:8080/api/profile/me
Authorization: Bearer invalid_token_here
```

**Ожидается: 401 Unauthorized**

---

## 🔍 Проверка в браузере

### localStorage

1. Откройте DevTools (F12)
2. Перейдите в **Application → Local Storage**
3. Найдите **http://localhost:3001**
4. Должен быть ключ `token` с JWT токеном

### Network запросы

1. Откройте DevTools (F12) → Network
2. Выполните вход
3. Найдите запрос `POST /api/auth/login`
4. Проверьте:
   - **Request:** username, password
   - **Response:** token, userId, username, email, role

5. Перейдите на защищённую страницу
6. Найдите любой API запрос
7. Проверьте заголовок:
   ```
   Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

### React DevTools

1. Установите React DevTools (расширение Chrome/Firefox)
2. Откройте DevTools → Components
3. Найдите `<AuthProvider>`
4. Проверьте состояние:
   ```
   user: { userId: 1, username: "testuser", email: "test@example.com", role: "USER" }
   token: "eyJhbGci..."
   loading: false
   isAuthenticated: true
   ```

---

## 📊 Проверка в БД (PostgreSQL)

### Подключитесь к БД

```bash
psql -U postgres -d movie_aggregator
```

### Проверьте пользователей

```sql
SELECT id, username, email, role, registration_date 
FROM users 
ORDER BY registration_date DESC;
```

**Пример результата:**
```
 id | username | email              | role | registration_date  
----+----------+--------------------+------+-------------------
  1 | testuser | test@example.com   | USER | 2025-10-01 14:30:00
  2 | apiuser  | api@example.com    | USER | 2025-10-01 14:35:00
```

### Проверьте хеширование пароля

```sql
SELECT username, password FROM users LIMIT 1;
```

**Пароль должен быть хешем BCrypt:**
```
username | password
---------+-------------------------------------------------------
testuser | $2a$10$abcdef123456...hashed_password_here
```

### Создайте тестового пользователя через SQL

```sql
INSERT INTO users (
  username, email, password, role, level, 
  registration_date, total_reviews, total_ratings, reputation
) VALUES (
  'demo',
  'demo@cinemahub.com',
  '$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy', -- BCrypt hash for "demo123"
  'USER',
  'NOVICE',
  NOW(),
  0, 0, 0
);
```

Теперь можете войти с:
```
Username: demo
Password: demo123
```

---

## ✅ Чек-лист функциональности

### Backend
- [x] POST /api/auth/register - Регистрация работает
- [x] POST /api/auth/login - Вход работает
- [x] GET /api/auth/validate - Валидация токена работает
- [x] Пароли хешируются с BCrypt
- [x] JWT токены генерируются
- [x] Токены валидируются в JwtRequestFilter
- [x] CORS настроен для localhost:3001
- [x] Защищённые эндпоинты требуют токен
- [x] Публичные эндпоинты доступны без токена

### Frontend
- [x] Страница /login отображается
- [x] Страница /register отображается
- [x] Форма регистрации валидирует данные
- [x] Индикаторы валидации пароля работают
- [x] Успешный вход сохраняет токен
- [x] Успешная регистрация автоматически входит
- [x] AuthContext предоставляет user и isAuthenticated
- [x] Токен добавляется ко всем запросам (Axios interceptor)
- [x] ProtectedRoute редиректит на /login
- [x] Navbar показывает username при входе
- [x] Кнопка "Выход" удаляет токен
- [x] После выхода редирект на главную

---

## 🎓 Готовность к сдаче курсовой

### Демонстрация преподавателю

1. **Показать регистрацию:**
   - Заполнить форму
   - Показать валидацию в реальном времени
   - Успешная регистрация

2. **Показать вход:**
   - Выход из аккаунта
   - Вход с теми же данными
   - Появление username в навигации

3. **Показать защищённые маршруты:**
   - Попытка открыть /profile без входа → редирект
   - Вход в систему
   - Успешное открытие /profile

4. **Показать Network запросы в DevTools:**
   - POST /api/auth/login
   - Заголовок Authorization в последующих запросах
   - JWT токен в localStorage

5. **Показать БД:**
   - Запрос SELECT * FROM users
   - Хешированные пароли
   - Данные пользователей

### Объяснить технологии

- **JWT (JSON Web Token):** Для stateless аутентификации
- **BCrypt:** Для безопасного хеширования паролей
- **Spring Security:** Для защиты backend эндпоинтов
- **React Context API:** Для глобального состояния авторизации
- **Axios Interceptors:** Для автоматического добавления токенов
- **Protected Routes:** Для ограничения доступа к страницам

---

## 🎉 Готово!

Система регистрации и авторизации полностью работает и готова к демонстрации!

**Создано:**
- 9 backend файлов (Security, Controllers, DTOs, Services, Repositories)
- 4 frontend файла (AuthContext, Login, Register, ProtectedRoute)
- Полная документация (AUTH-SYSTEM.md)
- Инструкция по тестированию (TESTING-AUTH.md)

**Можете:**
- ✅ Регистрировать новых пользователей
- ✅ Входить в систему
- ✅ Защищать страницы и API
- ✅ Автоматически добавлять токены к запросам
- ✅ Выходить из системы

**Успехов в защите курсовой!** 🎓🔐
