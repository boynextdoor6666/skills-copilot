# Презентация проекта CineVibe
## Агрегатор фильмов, сериалов и игр

---

## СЛАЙД 1: Титульный слайд

### CineVibe - Универсальная платформа развлечений

**Подзаголовок:**
Агрегатор фильмов, сериалов и игр с интеллектуальной системой рецензий

**Информация о проекте:**
- Веб-приложение для оценки и обсуждения развлекательного контента
- Объединяет профессиональные рецензии критиков и мнения пользователей
- Современный стек технологий: React + NestJS + MySQL

**Автор:** [Ваше имя]  
**Год:** 2025

---

## СЛАЙД 2: Актуальность и проблематика

### Проблема

**Фрагментация информации:**
- Пользователи вынуждены переключаться между множеством сайтов (IMDb, Metacritic, Steam, IGN)
- Отсутствие единой платформы для всех типов развлекательного контента
- Сложность в принятии решения о просмотре/покупке

**Недостаток доверия:**
- Разрозненность оценок критиков и пользователей
- Отсутствие системы персонализации рецензий
- Невозможность отследить, оправдались ли ожидания

### Решение - CineVibe

**Универсальная платформа** объединяющая:
✅ Фильмы, сериалы и игры в одном месте  
✅ Профессиональные рецензии и пользовательские отзывы  
✅ Систему "До/После" для сравнения ожиданий и реальности  
✅ Геймификацию для повышения вовлеченности  

---

## СЛАЙД 3: Функциональные возможности

### Для всех пользователей (Гости)
📌 **Просмотр контента**
- Каталог фильмов, сериалов и игр
- Поиск и фильтрация по жанрам, годам, рейтингам
- Детальная информация о каждом произведении

📊 **Аналитика и рейтинги**
- Metascore критиков (профессиональные оценки)
- User Score (оценки зрителей)
- Распределение положительных/нейтральных/негативных отзывов

### Для авторизованных пользователей
✍️ **Публикация отзывов**
- Написание собственных рецензий с оценкой 1-10
- Система голосования (лайки/дизлайки)
- Эмоциональная оценка контента

🎯 **Персональные функции**
- Список "Хочу посмотреть/поиграть"
- Система "До/После" - оценка ожиданий vs реальность
- Профиль пользователя с историей активности

🏆 **Геймификация**
- Система опыта (XP) и уровней
- Достижения за активность (рецензии, оценки)
- Таблица лидеров

### Для критиков
⭐ **Профессиональные рецензии**
- Публикация экспертных обзоров
- Привязка к изданиям (IGN, GameSpot, etc.)
- Система подписок и фолловинга

### Для администраторов
🛡️ **Управление платформой**
- Модерация отзывов и пользователей
- Управление героическим каруселью
- Статистика и аналитика платформы

---

## СЛАЙД 4: Архитектура системы

### Трёхуровневая архитектура
```
┌─────────────────┐
│   Клиентский    │  React 18 + Vite + Tailwind CSS
│     уровень     │  SPA (Single Page Application)
└────────┬────────┘
         │ HTTPS / REST API / JWT
┌────────▼────────┐
│    Уровень      │  NestJS 10 + TypeScript 5
│   приложений    │  Swagger API, Winston Logging
└────────┬────────┘
         │ TypeORM / TCP/IP
┌────────▼────────┐
│    Уровень      │  MySQL 8.0 + InnoDB
│     данных      │  Хранимые процедуры, триггеры
└─────────────────┘
```

### Frontend (Клиентская часть)
- **React 18** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **Tailwind CSS** - utility-first CSS фреймворк
- **React Router v6** - маршрутизация
- **Axios** - HTTP клиент
- **Lucide Icons** - иконки

**Ключевые компоненты:**
- MetascoreBadge - бейдж оценок критиков
- UserScoreBadge - бейдж оценок пользователей
- ExpectationsWidget - виджет "До/После"
- CriticReviewCard - карточка рецензии критика
- ReviewDistribution - распределение отзывов

### Backend (Серверная часть)
- **NestJS 10** - прогрессивный Node.js фреймворк
- **TypeScript 5** - типизированный JavaScript
- **TypeORM** - ORM для работы с БД
- **JWT + bcrypt** - аутентификация и хеширование
- **Winston** - структурированное логирование
- **Swagger/OpenAPI** - документация API

**Модульная архитектура:**
- AuthModule, UsersModule, ContentModule
- ReviewsModule, CriticsModule, AdminModule
- GamificationModule (XP, достижения)
- ExpectationsModule (До/После система)
- ExternalApiModule (TMDB, IGDB)

### Внешние интеграции
- **TMDB API** - данные о фильмах и сериалах
- **IGDB API** - данные об играх (Twitch OAuth 2.0)

---

## СЛАЙД 5: Уникальные возможности

### 1. Система "До/После" (Expectations)
**Инновационная функция оценки ожиданий**

Пользователь может:
1. **ДО просмотра/игры** - оценить свои ожидания (1-10)
2. **ПОСЛЕ** - оставить реальную оценку
3. **Сравнить** разницу между ожиданиями и реальностью
---

## СЛАЙД 5: База данных - структура и организация

### Общая информация
- **СУБД:** MySQL 8.0 (Community Edition)
- **Storage Engine:** InnoDB (транзакционный движок)
- **Кодировка:** UTF8MB4 (полная поддержка Unicode, эмодзи)
- **Collation:** utf8mb4_unicode_ci

### Структура базы данных

**Всего: 20+ таблиц, 4 основные категории**

#### 1. Основные таблицы (Core Tables)
```
users                  - Пользователи системы
├─ id (PK)
├─ username (UNIQUE)
├─ email (UNIQUE)
├─ password_hash
├─ role (ENUM: USER, CRITIC, ADMIN)
├─ avatar_url
└─ created_at

content               - Универсальная таблица контента
├─ id (PK)
├─ title
├─ type (ENUM: MOVIE, SERIES, GAME)
├─ release_year
├─ genre (JSON)
├─ poster_url, cover_url
├─ description
├─ critics_rating (DECIMAL)
├─ audience_rating (DECIMAL)
├─ reviews_count
├─ platforms (JSON) - для игр
├─ total_seasons - для сериалов
└─ system_requirements (JSON) - для игр

reviews               - Рецензии пользователей
├─ id (PK)
├─ content_id (FK → content)
├─ user_id (FK → users)
├─ rating (DECIMAL 3,1)
├─ content (TEXT)
├─ aspects (JSON)
├─ emotions (JSON)
└─ created_at

review_votes          - Голосование за отзывы
├─ id (PK)
├─ user_id (FK → users)
├─ review_id (FK → reviews)
├─ vote_type (ENUM: LIKE, DISLIKE)
└─ UNIQUE(user_id, review_id)

user_ratings          - Оценки контента
├─ user_id (FK → users)
├─ content_id (FK → content)
├─ rating (DECIMAL)
└─ PRIMARY KEY (user_id, content_id)

watchlist             - Список "Хочу посмотреть"
├─ id (PK)
├─ user_id (FK → users)
├─ content_id (FK → content)
└─ added_at
---

## СЛАЙД 8: Результаты разработкиn Tables)
```
achievements          - Доступные достижения
├─ id (PK)
├─ name
├─ description
├─ type (review_count, rating_value, etc.)
├─ threshold
├─ icon_url
└─ xp_reward

user_achievements     - Достижения пользователей
├─ id (PK)
├─ user_id (FK → users)
├─ achievement_id (FK → achievements)
├─ unlocked_at
└─ UNIQUE(user_id, achievement_id)

user_statistics       - Статистика пользователей
├─ user_id (PK, FK → users)
├─ total_reviews
├─ total_ratings
├─ total_xp
├─ level
├─ achievements_count
└─ updated_at

notifications         - Уведомления
├─ id (PK)
├─ user_id (FK → users)
├─ type (achievement, level_up, etc.)
├─ message
├─ is_read
└─ created_at
```

#### 3. Контент и медиа (Content & Media Tables)
```
hero_carousel         - Карусель на главной
├─ id (PK)
├─ content_id (FK → content)
├─ title
├─ description
├─ image_url
├─ order_position
└─ is_active

coming_soon_items     - Скоро выйдет
├─ id (PK)
├─ content_id (FK → content)
├─ release_date
├─ hype_level
└─ is_active

publications          - Издания критиков
├─ id (PK)
├─ name (IGN, GameSpot, etc.)
├─ logo_url
└─ website_url

expectations          - Система "До/После"
├─ id (PK)
├─ user_id (FK → users)
├─ content_id (FK → content)
├─ rating (DECIMAL 3,1)
├─ created_at
└─ UNIQUE(user_id, content_id)
```

#### 4. Критики и подписки (Critics Tables)
```
subscriptions         - Платные подписки
├─ id (PK)
---

## СЛАЙД 9: Перспективы развития и заключение
└─ is_active

user_follows          - Подписки на критиков
├─ id (PK)
├─ follower_id (FK → users)
├─ critic_id (FK → users)
├─ created_at
└─ UNIQUE(follower_id, critic_id)

user_critic_preferences - Предпочтения критиков
├─ id (PK)
├─ user_id (FK → users)
├─ critic_id (FK → users)
├─ weight (0.0-1.0)
└─ UNIQUE(user_id, critic_id)
```

### Индексы для оптимизации

**Основные индексы:**
- `idx_content_type` - на content.type (фильтрация по типу)
- `idx_content_rating` - на content.critics_rating (сортировка по рейтингу)
- `idx_content_year` - на content.release_year (фильтр по году)
- `idx_reviews_content` - на reviews.content_id (получение отзывов)
- `idx_reviews_user` - на reviews.user_id (отзывы пользователя)
- `idx_votes_review` - на review_votes.review_id (подсчет лайков)
- `idx_expectations_content` - на expectations.content_id (статистика ожиданий)

**Составные индексы:**
- UNIQUE(user_id, content_id) - предотвращение дублирования оценок
- UNIQUE(follower_id, critic_id) - предотвращение повторных подписок

### Foreign Keys и целостность данных

**Каскадное удаление (ON DELETE CASCADE):**
- При удалении пользователя → удаляются его отзывы, оценки, достижения
- При удалении контента → удаляются связанные отзывы и оценки
- При удалении отзыва → удаляются голоса за него

**Ограничения целостности:**
- NOT NULL для критичных полей (user_id, content_id, rating)
- CHECK constraints для диапазонов (rating BETWEEN 0 AND 10)
- UNIQUE constraints для предотвращения дублей

---

## СЛАЙД 6: База данных - хранимые процедуры и триггеры

### Хранимые процедуры (Stored Procedures)

#### 1. Процедуры для рецензий
```sql
-- Добавление отзыва зрителя
PROCEDURE add_review_viewer(
  IN p_user_id INT,
  IN p_content_id INT,
  IN p_aspects JSON,
  IN p_emotions JSON,
  IN p_rating DECIMAL(3,1),
  IN p_review_text TEXT
)
```
**Логика:**
- Вставляет новый отзыв в таблицу reviews
- Обновляет счетчик отзывов в content
- Пересчитывает средний рейтинг контента
- Начисляет XP пользователю
- Проверяет достижения

```sql
-- Публикация профессиональной рецензии
PROCEDURE publish_pro_review(
  IN p_content_id INT,
  IN p_critic_id INT,
  IN p_review_text TEXT,
  IN p_aspects JSON,
  IN p_emotions JSON,
  IN p_rating DECIMAL(3,1)
)
```
**Особенности:**
- Только для пользователей с ролью CRITIC
- Повышенное начисление XP (15 вместо 5)
- Обновляет critics_rating отдельно от audience_rating

#### 2. Административные процедуры
```sql
-- Удаление отзыва администратором
PROCEDURE admin_delete_review(
  IN p_review_id INT,
  IN p_admin_id INT,
  IN p_reason VARCHAR(255)
)
```
**Действия:**
- Проверяет права администратора
- Логирует причину удаления
- Удаляет отзыв
- Пересчитывает статистику контента
- Создает уведомление автору отзыва

```sql
-- Получение статистики контента
PROCEDURE get_content_stats(
  IN p_content_id INT,
  OUT p_total_reviews INT,
  OUT p_avg_rating DECIMAL(3,1),
  OUT p_critics_rating DECIMAL(3,1),
  OUT p_audience_rating DECIMAL(3,1)
)
```

#### 3. Процедуры для геймификации
```sql
-- Начисление XP и проверка уровня
PROCEDURE award_xp(
  IN p_user_id INT,
  IN p_xp_amount INT
)
```
**Логика:**
- Добавляет XP к user_statistics
- Проверяет, достигнут ли новый уровень
- Если да - повышает уровень
- Создает уведомление о повышении

```sql
-- Разблокировка достижения
PROCEDURE unlock_achievement(
  IN p_user_id INT,
  IN p_achievement_id INT
)
```
**Проверки:**
- Достижение еще не разблокировано
- Выполнены условия достижения
- Начисляет XP награды
- Обновляет счетчик достижений

### Триггеры (Triggers)

#### 1. Триггеры на таблице reviews

```sql
-- После вставки отзыва
TRIGGER after_review_insert
AFTER INSERT ON reviews
FOR EACH ROW
BEGIN
  -- Обновить счетчик отзывов
  UPDATE content 
  SET reviews_count = reviews_count + 1
  WHERE id = NEW.content_id;
  
  -- Пересчитать средний рейтинг
  UPDATE content c
  SET c.audience_rating = (
    SELECT AVG(r.rating) 
    FROM reviews r 
    WHERE r.content_id = c.id
  )
  WHERE c.id = NEW.content_id;
  
  -- Обновить статистику пользователя
  UPDATE user_statistics
  SET total_reviews = total_reviews + 1
  WHERE user_id = NEW.user_id;
END;
```

```sql
-- После удаления отзыва
TRIGGER after_review_delete
AFTER DELETE ON reviews
FOR EACH ROW
BEGIN
  -- Уменьшить счетчик
  UPDATE content 
  SET reviews_count = reviews_count - 1
  WHERE id = OLD.content_id;
  
  -- Пересчитать рейтинг
  UPDATE content c
  SET c.audience_rating = COALESCE(
    (SELECT AVG(r.rating) FROM reviews r WHERE r.content_id = c.id),
    0
  )
  WHERE c.id = OLD.content_id;
END;
```

#### 2. Триггеры на таблице user_ratings

```sql
-- После обновления оценки
TRIGGER after_rating_update
AFTER UPDATE ON user_ratings
FOR EACH ROW
BEGIN
  -- Пересчитать audience_rating
  UPDATE content
  SET audience_rating = (
    SELECT AVG(rating) 
    FROM user_ratings 
    WHERE content_id = NEW.content_id
  )
  WHERE id = NEW.content_id;
END;
```

#### 3. Триггеры геймификации

```sql
-- После разблокировки достижения
TRIGGER after_achievement_unlock
AFTER INSERT ON user_achievements
FOR EACH ROW
BEGIN
  -- Увеличить счетчик достижений
  UPDATE user_statistics
  SET achievements_count = achievements_count + 1
  WHERE user_id = NEW.user_id;
  
  -- Создать уведомление
  INSERT INTO notifications (user_id, type, message)
  VALUES (NEW.user_id, 'achievement', 
          CONCAT('Получено достижение: ', 
                 (SELECT name FROM achievements WHERE id = NEW.achievement_id)));
END;
```

### Представления (Views) для аналитики

```sql
-- Топ контента по рейтингу критиков
CREATE VIEW top_content_by_metascore AS
SELECT 
  c.id,
  c.title,
  c.type,
  c.critics_rating as metascore,
  c.reviews_count,
  c.poster_url
FROM content c
WHERE c.critics_rating > 0
ORDER BY c.critics_rating DESC
LIMIT 100;
```

```sql
-- Активные пользователи с высоким уровнем
CREATE VIEW top_users AS
SELECT 
  u.id,
  u.username,
  us.level,
  us.total_xp,
  us.total_reviews,
  us.achievements_count
FROM users u
JOIN user_statistics us ON u.id = us.user_id
ORDER BY us.total_xp DESC;
```

### Оптимизация производительности

**Применённые техники:**

1. **Денормализация**
   - critics_rating, audience_rating хранятся в content (избегаем JOIN при каждом запросе)
   - reviews_count кэшируется вместо COUNT(*)

2. **JSON поля**
   - aspects, emotions в reviews
   - genre, platforms, system_requirements в content
   - Баланс между нормализацией и гибкостью

3. **Индексы на часто используемых полях**
   - Foreign keys автоматически индексируются
   - Дополнительные индексы на поля фильтрации и сортировки

4. **Batch операции**
   - Хранимые процедуры объединяют множество операций
   - Триггеры автоматизируют обновления

5. **Partitioning (планируется)**
   - Разделение reviews по годам
   - Архивирование старых данных

---

## СЛАЙД 7: Уникальные возможностиожидания
- Показывает статистику сообщества (средние ожидания)
- Визуализация: "Превзошёл ожидания" / "Ниже ожиданий"

**Пример:**
```
Ожидания:    7.5/10  ⭐
Реальность:  9.0/10  ⭐⭐
Разница:     +1.5    📈 Превзошёл ожидания!
```

### 2. Геймификация с достижениями
**Мотивация пользователей через игровые механики**

**Система уровней:**
- Начальный уровень: 0 XP
- Получение XP за активность (отзывы, оценки)
- Повышение уровня открывает новые возможности

**Достижения:**
- 🎬 "Первый шаг" - первая рецензия
- ⭐ "Критик" - 10 рецензий
- 📚 "Эксперт" - 50 рецензий
- 🏆 "Легенда" - 100 рецензий
- 📝 "Писатель" - рецензия длиной 500+ символов

**Таблица лидеров:**
- Топ пользователей по XP
- Статистика за неделю/месяц/всё время

### 3. Специализированные страницы контента
**Уникальные поля для каждого типа**

**Для игр (GameDetail):**
- 🎮 Платформы (PC, PS5, Xbox, Switch)
- 💻 Системные требования (минимальные/рекомендуемые)
- 🌍 Поддерживаемые языки
- 👥 Разработчик и издатель
- 💾 Размер установки

**Для сериалов (SeriesDetail):**
- 📺 Сезоны и эпизоды
- 📡 Телеканал/платформа (Netflix, HBO, AMC)
- ⏱️ Длительность эпизода
- 🎭 Создатели и актёрский состав
- 📊 Статус (В производстве, Завершён, Отменён)

**Для фильмов (DetailedContentPage):**
- 🎬 Режиссёр, актёры, продюсеры
- 💰 Бюджет и кассовые сборы
- 🏆 Награды и номинации
- 🎥 Трейлеры и постеры

### 4. Цветовая кодировка в стиле Metacritic
**Визуальная дифференциация оценок**

- **🟢 Зелёный (7.0-10.0)** - Положительные отзывы
- **🟡 Жёлтый (4.0-6.9)** - Нейтральные отзывы
- **🔴 Красный (0.0-3.9)** - Негативные отзывы

Применяется к:
- Оценкам критиков (Metascore)
- Оценкам пользователей (User Score)
- Рамкам отзывов (левая граница)
- Бейджам с оценками

---

## СЛАЙД 6: Результаты разработки

### Реализованная функциональность

#### ✅ Frontend (100%)
- ✔ Адаптивный дизайн (desktop/mobile)
- ✔ Маршрутизация (React Router v6)
- ✔ Аутентификация (JWT)
- ✔ Каталог контента с фильтрами
- ✔ Детальные страницы (фильмы, сериалы, игры)
- ✔ Система отзывов и рецензий
- ✔ Профиль пользователя
- ✔ Админ-панель
- ✔ Виджет "До/После"

#### ✅ Backend (100%)
- ✔ RESTful API (20+ эндпоинтов)
- ✔ Swagger документация
- ✔ JWT аутентификация + Role-based доступ
- ✔ TypeORM интеграция
- ✔ Хранимые процедуры MySQL
- ✔ Winston логирование
- ✔ Геймификация (XP, достижения)
- ✔ Внешние API (TMDB, IGDB)
- ✔ Обработка ошибок и валидация

#### ✅ База данных (100%)
- ✔ 20+ таблиц с индексами
- ✔ Foreign keys и constraints
- ✔ Хранимые процедуры (10+)
- ✔ Триггеры для автоматизации
- ✔ Оптимизация запросов

#### ✅ Big Data & ML (Beta)
- ✔ **Apache Kafka** - брокер сообщений для стриминга событий
- ✔ **ClickHouse** - OLAP СУБД для аналитики в реальном времени
- ✔ **Apache Spark** - обработка больших данных (ETL)
- ✔ **Scikit-learn** - ML алгоритмы (Cosine Similarity)
- ✔ **Hybrid Recommender** - гибридная система рекомендаций

### Показатели проекта

📊 **Статистика кода:**
- **Frontend:** ~15,000 строк (JSX/JS)
- **Backend:** ~8,000 строк (TypeScript)
- **SQL:** ~2,000 строк (процедуры, схемы)
- **Всего:** ~25,000 строк кода

📁 **Структура:**
- 50+ React компонентов
- 10 страниц
- 12 NestJS модулей
- 20+ таблиц БД

🎨 **UI/UX:**
- Современный темный дизайн
- Цветовая схема Metacritic
- Lucide Icons
- Плавные анимации и переходы

### Преимущества решения

✨ **Для пользователей:**
- Единая платформа для всех типов контента
- Прозрачная система оценок
- Персонализация через "До/После"
- Геймификация повышает вовлеченность

🎯 **Для бизнеса:**
- Масштабируемая архитектура
- Модульная структура (легкое добавление функций)
- Документированный API (Swagger)
- Логирование для мониторинга

💻 **Технические:**
- TypeScript - типобезопасность
- NestJS - enterprise-ready фреймворк
- MySQL - надёжность и производительность
- JWT - безопасная аутентификация

---

## СЛАЙД 7: Перспективы развития и заключение

### Планы развития (Roadmap)

#### 📈 Фаза 2: Real-time аналитика
**Цель:** Обработка данных в реальном времени

**Технологии:**
- Apache Kafka - очередь событий
- Apache Spark (PySpark) - потоковая обработка
- ClickHouse - аналитическая СУБД

**Новые возможности:**
- Трендовые фильмы/игры в режиме реального времени
- Эмоциональные облака тегов
- Карты восприятия контента
- Динамика рейтингов по времени

#### 🌍 Фаза 3: Социальные функции
- Друзья и подписки
- Личные сообщения
- Обсуждения и комментарии
- Рекомендации на основе друзей

#### 🤖 Фаза 4: Машинное обучение
- Персонализированные рекомендации
- Предсказание оценок пользователей
- Автоматическая категоризация контента
- Определение токсичности отзывов

#### 📱 Фаза 5: Мобильное приложение
- Native iOS/Android приложения
- Push-уведомления
- Офлайн-режим
- Интеграция с умными устройствами

### Технические улучшения

**Производительность:**
- Redis кэширование
- CDN для статических файлов
- Оптимизация SQL запросов
- Lazy loading компонентов

**Безопасность:**
- OAuth 2.0 (Google, Facebook)
- Two-factor authentication (2FA)
- Rate limiting API
- CSRF защита

**Мониторинг:**
- Prometheus + Grafana
- ELK Stack (Elasticsearch, Logstash, Kibana)
- Sentry для отслеживания ошибок

### Заключение

#### Достижения проекта

✅ **Создана полнофункциональная платформа** для агрегации и оценки развлекательного контента

✅ **Реализована уникальная система "До/После"** для анализа ожиданий

✅ **Разработана современная архитектура** с использованием лучших практик:
- Модульность и расширяемость
- Типобезопасность (TypeScript)
- RESTful API с документацией
- Масштабируемая структура БД

✅ **Интегрированы внешние API** (TMDB, IGDB) для автоматического импорта контента

✅ **Внедрена геймификация** для повышения вовлеченности пользователей

#### Практическая значимость

🎯 **Для пользователей:**
Экономия времени на поиск информации о контенте, помощь в принятии решений о просмотре/покупке

🏢 **Для индустрии:**
Агрегация мнений критиков и зрителей, инструмент для продвижения контента

📚 **Для образования:**
Демонстрация современных веб-технологий и архитектурных паттернов

### Контакты и благодарности

**Проект:** CineVibe - Movie Aggregator Platform  
**GitHub:** github.com/boynextdoor6666/skills-copilot  
**Технологии:** React, NestJS, MySQL, TypeScript  

**Спасибо за внимание!**

---

### Дополнительная информация для выступления

**Время презентации:** 10-15 минут

**Структура выступления:**
1. **Слайд 1-2** (2-3 мин): Введение, актуальность проблемы
2. **Слайд 3** (3-4 мин): Функциональные возможности, демонстрация
3. **Слайд 4** (2-3 мин): Техническая архитектура
4. **Слайд 5** (3-4 мин): Уникальные фичи (с демо)
5. **Слайд 6** (2 мин): Результаты и метрики
6. **Слайд 7** (2 мин): Планы развития, заключение

**Советы для презентации:**
- Подготовьте живую демонстрацию системы
- Покажите виджет "До/После" в действии
- Продемонстрируйте цветовую кодировку отзывов
- Откройте Swagger документацию API
- Покажите систему достижений
- Будьте готовы ответить на вопросы о технической реализации
