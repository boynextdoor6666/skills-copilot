# 🏗️ Архитектура CinemaHub

## 📐 Общая структура

```
┌─────────────────────────────────────────────────────────────┐
│                        ПОЛЬЗОВАТЕЛЬ                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   FRONTEND (React + Vite)                    │
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Фильмы  │  │ Сериалы  │  │   Игры   │  │ Профиль  │   │
│  │ Movies   │  │  Series  │  │  Games   │  │ Profile  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Navbar • Router • Компоненты              │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
│  Design: Tailwind CSS • IMDb/Metacritic Style              │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP/REST
                         │ (CORS, JSON)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              BACKEND (Spring Boot 3.2)                       │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                   REST Controllers                      │ │
│  │  MovieController • UserController • ReviewController   │ │
│  └───────────────────────┬────────────────────────────────┘ │
│                          │                                   │
│  ┌───────────────────────▼────────────────────────────────┐ │
│  │                    Service Layer                        │ │
│  │  MovieService • UserService • ReviewService • NLP      │ │
│  └───────────────────────┬────────────────────────────────┘ │
│                          │                                   │
│  ┌───────────────────────▼────────────────────────────────┐ │
│  │                  Repository Layer                       │ │
│  │  MovieRepository • UserRepository • ReviewRepository   │ │
│  └───────────────────────┬────────────────────────────────┘ │
│                          │ JPA/Hibernate                    │
└──────────────────────────┼──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  PostgreSQL Database                         │
│                                                               │
│  Tables: movies, users, reviews, ratings, achievements...   │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Типы контента

```
                    ┌─────────────┐
                    │   Content   │
                    └──────┬──────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
      ┌────────┐      ┌────────┐      ┌────────┐
      │ MOVIE  │      │TV_SERIES│     │  GAME  │
      └────────┘      └────────┘      └────────┘
           │               │               │
      ┌────┴────┐     ┌────┴────┐    ┌────┴────┐
      │director │     │ creator │    │developer│
      │  cast   │     │ seasons │    │platforms│
      │ runtime │     │episodes │    │publisher│
      └─────────┘     └─────────┘    └─────────┘
```

## 📊 Модель данных

```
┌──────────────────────────────────────────────────────────────┐
│                          MOVIE                                │
├──────────────────────────────────────────────────────────────┤
│ id: Long                                                      │
│ title: String                                                 │
│ contentType: MOVIE | TV_SERIES | GAME                       │
│ genres: List<Genre>                                          │
│ releaseYear: Integer                                         │
├──────────────────────────────────────────────────────────────┤
│ RATINGS:                                                      │
│ - overallRating: Double                                      │
│ - criticsRating: Double                                      │
│ - audienceRating: Double                                     │
│ - hypeIndex: Integer                                         │
├──────────────────────────────────────────────────────────────┤
│ UNIQUE FEATURES:                                             │
│ - emotionCloud: Map<String, Integer>                        │
│   {joy: 60, sadness: 30, tension: 45...}                    │
│                                                               │
│ - perceptionMap: Map<String, Double>                        │
│   {plot: 8.5, acting: 9.0, visuals: 8.8...}                │
│                                                               │
│ - countryRatings: Map<String, Double>                       │
│   {US: 8.9, JP: 8.2, RU: 8.7...}                           │
│                                                               │
│ - preWatchExpectation: Double                               │
│ - postWatchRating: Double                                    │
├──────────────────────────────────────────────────────────────┤
│ SPECIFIC FIELDS:                                             │
│ For TV_SERIES: seasonsCount, episodesCount, creator         │
│ For GAME: platforms[], developer, publisher                 │
└──────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴──────────┐
                    │                    │
                    ▼                    ▼
            ┌──────────────┐     ┌──────────────┐
            │    REVIEW    │     │  USER_RATING │
            ├──────────────┤     ├──────────────┤
            │ userId       │     │ userId       │
            │ movieId      │     │ movieId      │
            │ content      │     │ rating       │
            │ emotionScores│     │ ratingType   │
            │ sentimentScore│    │ watchStatus  │
            └──────────────┘     └──────────────┘
```

## 🔄 Поток данных

### 1️⃣ Получение списка контента

```
User clicks "Фильмы"
        │
        ▼
   Movies.jsx
        │
        ├─► useState (viewMode, sortBy, filterGenre)
        │
        ├─► filteredMovies = movies.filter().sort()
        │
        └─► Render grid/list view
                │
                └─► MovieCard components
                        │
                        └─► Link to /movie/{id}
```

### 2️⃣ Детальная страница

```
User clicks Movie Card
        │
        ▼
  MovieDetail.jsx
        │
        ├─► useParams() → get movie id
        │
        ├─► Fetch movie data (будущее API call)
        │
        └─► Render:
            ├─► Hero section (poster, title, ratings)
            ├─► Emotion cloud
            ├─► Perception map
            ├─► Country ratings
            ├─► Reviews
            └─► Related content
```

### 3️⃣ NLP Анализ эмоций (Планируется)

```
User submits Review
        │
        ▼
  POST /api/reviews
        │
        ▼
  ReviewService
        │
        ├─► Save review to DB
        │
        ├─► NLPService.analyzeEmotions(reviewText)
        │       │
        │       └─► Stanford CoreNLP
        │               │
        │               └─► {joy: 0.7, sadness: 0.2...}
        │
        └─► Update Movie.emotionCloud
                │
                └─► Return updated movie data
```

## 🎨 Frontend компоненты

```
App.jsx
  │
  ├─► Navbar.jsx
  │     ├─► Logo
  │     ├─► Search bar
  │     └─► Navigation links
  │
  └─► Routes
        ├─► Home.jsx
        │     ├─► Hero section
        │     ├─► Features grid
        │     ├─► Trending movies
        │     ├─► Trending series
        │     ├─► Trending games
        │     └─► Statistics
        │
        ├─► Movies.jsx
        │     ├─► Filter bar
        │     └─► Movie cards grid/list
        │
        ├─► Series.jsx
        │     ├─► Filter bar
        │     └─► Series cards grid/list
        │
        ├─► Games.jsx
        │     ├─► Filter bar
        │     └─► Game cards grid/list
        │
        ├─► MovieDetail.jsx
        │     ├─► Hero
        │     ├─► Details
        │     ├─► Emotion analysis
        │     ├─► Perception map
        │     └─► Reviews
        │
        └─► Profile.jsx
              ├─► User info
              ├─► Ratings
              ├─► Achievements
              └─► Statistics
```

## 🔐 Безопасность (Планируется)

```
User login request
        │
        ▼
  POST /api/auth/login
        │
        ▼
  AuthController
        │
        ├─► Validate credentials
        │
        ├─► Generate JWT token
        │     ├─► Header
        │     ├─► Payload (userId, roles)
        │     └─► Signature
        │
        └─► Return token
                │
                └─► Frontend stores in localStorage
                        │
                        └─► Include in subsequent requests:
                            Authorization: Bearer <token>
```

## 📱 Адаптивность

```
Desktop (≥1024px)
├─► Full navigation bar
├─► Grid view: 4 columns
├─► Side panels
└─► Rich details

Tablet (768px - 1023px)
├─► Compact navigation
├─► Grid view: 2-3 columns
└─► Responsive layout

Mobile (<768px)
├─► Hamburger menu
├─► Grid view: 1-2 columns
├─► Bottom navigation
└─► Mobile-optimized cards
```

## 🚀 Deployment (Будущее)

```
Development
    │
    ├─► Frontend: localhost:3001 (Vite dev server)
    └─► Backend: localhost:8080 (Spring Boot)

Production
    │
    ├─► Frontend: Vercel / Netlify
    │     └─► npm run build → static files
    │
    └─► Backend: Heroku / Railway / AWS
          ├─► Docker container
          └─► PostgreSQL database
```

## 🎯 Следующие шаги реализации

1. **Backend API** ⏳
   ```
   Repository Layer
        ↓
   Service Layer (+ NLP)
        ↓
   REST Controllers
        ↓
   Integration Tests
   ```

2. **Database Setup** ⏳
   ```
   PostgreSQL installation
        ↓
   Schema migration
        ↓
   Seed data
   ```

3. **Frontend Integration** ⏳
   ```
   Axios/Fetch setup
        ↓
   Replace mock data with API calls
        ↓
   Error handling
        ↓
   Loading states
   ```

4. **Authentication** ⏳
   ```
   JWT implementation
        ↓
   Login/Register pages
        ↓
   Protected routes
        ↓
   User context
   ```

---

**CinemaHub Architecture** - Масштабируемая и расширяемая система 🏗️
